export enum IntervalEnum {
  MONTHLY = "MONTHLY",
  YEARLY = "YEARLY",
}

export enum StepEnum {
  PENDING = "PENDING",
  CONCLUDED = "CONCLUDED",
}

export enum TypeTemplateEnum {
  MOBILE = "MOBILE",
  DESKTOP = "DESKTOP",
}
